# OpenArc Assets

This directory is reserved for plugin UI assets such as icons, logos, and screenshots.

The initial plugin manifest does not reference asset files, so missing binary assets will not break local use.
